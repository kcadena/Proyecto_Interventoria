var express = require('express');
var Sequelize = require('sequelize');
var sqlCon = require('../config/connectionDb');
var fls = require('../model/Archivos');

var router = express.Router();

function downloadReport(id_usuario, keym) {
  
}
